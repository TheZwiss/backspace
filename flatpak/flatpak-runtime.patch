diff --git a/packages/desktop/package.json b/packages/desktop/package.json
index c2917fcb..ebea80ed 100644
--- a/packages/desktop/package.json
+++ b/packages/desktop/package.json
@@ -12,0 +13 @@
+  "desktopName": "io.github.TheZwiss.backspace.desktop",
diff --git a/packages/desktop/resources/recovery.html b/packages/desktop/resources/recovery.html
index 9697241a..e188e0f8 100644
--- a/packages/desktop/resources/recovery.html
+++ b/packages/desktop/resources/recovery.html
@@ -220 +220,4 @@
-      $('hint').textContent = hintFor(reasonCode, state.updateState);
+      const managedByFlatpak = window.backspace?.packageFormat === 'flatpak';
+      $('hint').textContent = managedByFlatpak
+        ? "Try reloading the app or changing the connected instance. Updates are managed by Flatpak."
+        : hintFor(reasonCode, state.updateState);
@@ -224,0 +228 @@
+      $('btn-check').style.display = managedByFlatpak ? 'none' : '';
@@ -227 +231 @@
-      $('btn-restart').style.display = state.updateState === 'downloaded' ? '' : 'none';
+      $('btn-restart').style.display = !managedByFlatpak && state.updateState === 'downloaded' ? '' : 'none';
diff --git a/packages/desktop/src/main.ts b/packages/desktop/src/main.ts
index 84ff0506..e83fff97 100644
--- a/packages/desktop/src/main.ts
+++ b/packages/desktop/src/main.ts
@@ -73,0 +74,4 @@ const knownInstanceOrigins = new Set<string>();
+// Flatpak deployments are immutable and are upgraded by Flatpak itself. Keep
+// electron-updater from downloading an AppImage/DEB that it cannot install.
+const IS_FLATPAK = Boolean(process.env.FLATPAK_ID);
+
@@ -596,0 +601 @@ function registerIpcHandlers(): void {
+    if (IS_FLATPAK) return;
@@ -605,0 +611 @@ function registerIpcHandlers(): void {
+    if (IS_FLATPAK) return;
@@ -623,0 +630,2 @@ function registerIpcHandlers(): void {
+    if (IS_FLATPAK) return { openAtLogin: false, startMinimized: false };
+
@@ -657,0 +666,2 @@ function registerIpcHandlers(): void {
+    if (IS_FLATPAK) return { openAtLogin: false, startMinimized: false };
+
@@ -725,0 +736,2 @@ function initAutoUpdater(): void {
+  if (IS_FLATPAK) return;
+
@@ -1017 +1029 @@ if (!gotTheLock) {
-        tray.setContextMenu(Menu.buildFromTemplate(buildTrayMenuTemplate(state, trayActions)));
+        tray.setContextMenu(Menu.buildFromTemplate(buildTrayMenuTemplate(state, trayActions, IS_FLATPAK)));
@@ -1020 +1032 @@ if (!gotTheLock) {
-        Menu.setApplicationMenu(Menu.buildFromTemplate(buildAppMenuTemplate(app.name, state, trayActions)));
+        Menu.setApplicationMenu(Menu.buildFromTemplate(buildAppMenuTemplate(app.name, state, trayActions, IS_FLATPAK)));
@@ -1034,3 +1046,5 @@ if (!gotTheLock) {
-    startActivityDetection((activity) => {
-      mainWindow?.webContents.send('activity-detected', activity);
-    });
+    if (!IS_FLATPAK) {
+      startActivityDetection((activity) => {
+        mainWindow?.webContents.send('activity-detected', activity);
+      });
+    }
diff --git a/packages/desktop/src/preload.ts b/packages/desktop/src/preload.ts
index 35730519..6210456b 100644
--- a/packages/desktop/src/preload.ts
+++ b/packages/desktop/src/preload.ts
@@ -5,0 +6 @@ contextBridge.exposeInMainWorld('backspace', {
+  packageFormat: process.env.FLATPAK_ID ? 'flatpak' : 'native',
diff --git a/packages/desktop/src/recovery.ts b/packages/desktop/src/recovery.ts
index 511c0677..661efbbd 100644
--- a/packages/desktop/src/recovery.ts
+++ b/packages/desktop/src/recovery.ts
@@ -122,0 +123 @@ export function buildTrayMenuTemplate(
+  updatesManagedExternally = false,
@@ -127,2 +127,0 @@ export function buildTrayMenuTemplate(
-    { type: 'separator' },
-    checkForUpdatesItem(state, () => actions?.onCheckForUpdates?.()),
@@ -131,7 +130,13 @@ export function buildTrayMenuTemplate(
-  if (state.updateState === 'downloaded') {
-    items.push({
-      id: 'restart-to-install',
-      label: 'Restart to Install Update',
-      enabled: true,
-      click: actions?.onRestartToInstall,
-    });
+  if (!updatesManagedExternally) {
+    items.push(
+      { type: 'separator' },
+      checkForUpdatesItem(state, () => actions?.onCheckForUpdates?.()),
+    );
+    if (state.updateState === 'downloaded') {
+      items.push({
+        id: 'restart-to-install',
+        label: 'Restart to Install Update',
+        enabled: true,
+        click: actions?.onRestartToInstall,
+      });
+    }
@@ -154,0 +160 @@ export function buildAppMenuTemplate(
+  updatesManagedExternally = false,
@@ -159,2 +164,0 @@ export function buildAppMenuTemplate(
-    { type: 'separator' },
-    checkForUpdatesItem(state, () => actions?.onCheckForUpdates?.()),
@@ -163,7 +167,13 @@ export function buildAppMenuTemplate(
-  if (state.updateState === 'downloaded') {
-    appSubmenu.push({
-      id: 'restart-to-install',
-      label: 'Restart to Install Update',
-      enabled: true,
-      click: actions?.onRestartToInstall,
-    });
+  if (!updatesManagedExternally) {
+    appSubmenu.push(
+      { type: 'separator' },
+      checkForUpdatesItem(state, () => actions?.onCheckForUpdates?.()),
+    );
+    if (state.updateState === 'downloaded') {
+      appSubmenu.push({
+        id: 'restart-to-install',
+        label: 'Restart to Install Update',
+        enabled: true,
+        click: actions?.onRestartToInstall,
+      });
+    }
