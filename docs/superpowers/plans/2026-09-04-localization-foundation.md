# Localization done properly, and the 1.0.6 release that precedes it

Written 2026-09-04 before a context compact. Everything below was verified in the
session that wrote it; the "open" items are marked.

## Status (2026-09-04, later the same day)

- Step 1 done. `v1.0.6` is tagged at `b3d570eb` (the first tag at `34e69f87`
  was deleted with its draft after the Flatpak prepare job failed on
  "dubious ownership" inside the container; fixed in #92). Release run green,
  draft release has 22 assets and written notes; Jannis publishes. The
  automation PR #94 (manifest pin, AppStream entry) is the gate for Flathub.
  Repo setting "Allow GitHub Actions to create and approve pull requests" was
  turned on for the last job of the chain; recorded in security-scanning.md.
- Steps 2 and 3 done: `docs/systems/localization.md` written and matches the
  code. Step 4.1 to 4.3 done on branch `feat/localization-foundation`:
  foundation plus the settings surface (all panels), date formatting across
  the app, desktop tray/menus/recovery/picker, users.ts error codes, the
  check script with a 90-file pending list.
- 4.5 done: #45 closed with credit, #33 updated. The foundation PR is #95.
- 4.4 in progress: eleven sweeps run in parallel as worktree forks branched
  from main after #95, one PR each: auth, chat-messages, chat-widgets, dm,
  voice, spaces-layout, spaces-modals, social, federation, admin, shell
  (uploads/mobile/desktop/common leftovers). Sweeps touch only their files
  and their namespace catalogs; `common.json`, `errors.json`, shared error
  codes and server routes are off limits to them (server error codes get a
  separate pass per route file afterwards, to avoid eleven-way conflicts).
  The pending list is regenerated by the maintainer at each merge.
- Then: flip `released` for ru and de in the PR that empties the pending
  list, German native pass, release 1.1.0.
- Follow-ups found during the work: keybind display labels are persisted
  in English at recording time (`keybindStore`), so they need deriving at
  render time in the keybinds sweep; `UpdatesPanel.formatCheckedAt` and
  `RegistrationPanel.formatRelative` build "X minutes ago" by hand (admin
  sweep); the DM system messages in `dmFormatters.ts` still render English
  (dm sweep); PR #93 (st7105, Flatpak webcam/UPower permissions) needs review.

## Decisions already made by Jannis

- Do the localization ourselves end to end (foundation plus the full surface
  sweep). Carry the contributor's Russian strings over with `Co-authored-by`
  credit. Close #45 with a proper thank-you. Do not ask the contributor to
  restructure it.
- Languages for the first localized release: English, Russian, German. Jannis
  wants a native pass on German before release; write it, flag it.
- Sequencing: release 1.0.6 first (main has #48 Flatpak and #90 policy), then
  localization lands as 1.1.0.
- Outward communication: plain human prose, no em dashes, no bullet-label slop.
  Be warm to contributors.

## Step 1: release 1.0.6

Procedure, taken from the 1.0.5 release (`29cb8ce8`, PR #88):

1. Branch from main. Run `node scripts/bump-version.mjs 1.0.6`. It sets the
   version in six manifests: root `package.json`, `packages/{desktop,server,
   shared,web}/package.json`, `scripts/metrics/package.json`.
   `test/version-consistency.test.ts` enforces they agree. Lockfile stays
   untouched (precedent from 1.0.3, 1.0.4, 1.0.5; workspace-internal versions).
2. Commit `chore: release 1.0.6`, open a PR whose body says "Version bump only"
   and points at the content PRs (#48, #90). Squash merge without `--subject`
   (GitHub appends `(#N)`; `--subject` suppresses it).
3. Tag the merge commit `v1.0.6` (lightweight tag on the commit, as previous
   releases are) and push the tag. That fires `release.yml`.
4. Watch `release.yml`. This is the first run of the new three-job Flatpak chain
   from #48: `prepare-flatpak-metadata` regenerates the pin, `node-sources.json`
   and the AppStream entry; `validate-flatpak-metadata` builds x86_64 and
   aarch64 from the result; `update-flatpak-metadata` opens
   `automation/flatpak-v1.0.6`. Review and merge that PR. Only after it merges
   may Flathub submission happen (memory: flatpak-packaging-pr48).
5. Write release notes on the GitHub release (the workflow creates a draft with
   an empty body). Plain prose, no em dashes.

## Step 2: what is wrong with #45 (feat(web): add localization support)

Branch fetched locally as `pr45`. 159 files, +7,468/−2,415. Author st7105
(same person as #48). Closes #33, which was opened by a different user, d00xD,
a Russian user running Backspace on a Raspberry Pi 5 because Discord is blocked
there. Thank both when closing/commenting.

What #45 gets right and we keep:
- `i18next` 25.5.2 + `react-i18next` 15.7.3. Right library: mature, JSON
  catalogs Weblate/Crowdin consume, CLDR plural suffixes for Russian.
- Detection order (stored choice, then browser languages), persisted under
  `backspace-language`, `document.documentElement.lang` kept in sync.
- A `check:i18n` script wired into `typecheck` and `build` that enforces
  catalog parity and `{{placeholder}}` consistency. Keep the idea, rewrite it
  against the new convention.
- The Russian translations themselves: 2,228 strings in
  `packages/web/src/locales/ru/translation.json`. Reuse the *values*, not the
  keys.

What is structurally wrong and must not be merged as is:
1. Keys are machine-generated and leak the extraction process. Evidence:
   `runtime.selected.dmFormatters.unknown` and `...unknown2`;
   `ui.JoinPage.yourPasswordIsVerifiedLocallyThenUsedTo` (the English sentence
   as the key); 2,151 of 2,228 keys under `ui.*`, `runtime.manual.*`,
   `runtime.selected.*` (tool categories, not meaning); scoped by source file
   name so a component rename invalidates its keys.
2. Plurals were wrapped, not restructured. Only 10 plural keys exist. A key
   `englishPluralSuffix_one/few/many/other` is `""` in Russian, meaning English
   still builds `file${n===1?'':'s'}` somewhere and Russian just blanks it.
   Wrong for Russian (1 файл / 2 файла / 5 файлов).
3. No typed keys. `translate(key: string)` accepts anything. No
   `CustomTypeOptions` resource typing.
4. All locales bundled eagerly (`import en`, `import ru` in `i18n.ts`,
   passed as `resources`). Every user downloads every language.
5. Scope stops at the web renderer. Nothing in `packages/server`,
   `packages/shared`, `packages/desktop` changed. Consequences: server error
   strings stay English (client does `err.message || translate(...)` and the
   server's English wins); desktop main-process strings (tray menu labels in
   `recovery.ts`, `recovery.html`, notification titles) stay English.
6. Dates: `formatDmTimestamp(createdAt, locale = 'en-US', yesterdayLabel =
   'Yesterday')` in #45 takes a locale parameter but defaults to en-US, so it
   is correct only if every caller passes `i18n.language`. Other date sites
   unknown (inventory grep failed on zsh globbing; redo with quoted
   `--include='*.tsx'`).
7. The language selector uses per-language keys (`language.english`,
   `language.russian`) when `supportedLanguages[].nativeName` already exists.

## Step 3: target architecture (the spec to build)

Write this into `docs/systems/localization.md` as the subsystem spec first,
then implement. Add the row to CLAUDE.md's subsystem table.

Library: keep `i18next` + `react-i18next`. Add nothing else unless a concrete
need appears (no ICU plugin needed; i18next plural suffixes cover ru/de/en).

Keys:
- Semantic, surface-scoped, never derived from English text or file names.
  Shape: `<surface>.<element>.<meaning>`, e.g. `chat.composer.placeholder`,
  `settings.desktop.autoLaunch.label`, `voice.controls.mute`. Surfaces are
  product areas (auth, chat, dm, voice, spaces, settings, admin, federation,
  social, search, uploads, desktop, mobile, common), not components.
- One namespace file per surface under `packages/web/src/locales/<lng>/`,
  so lazy loading and translator assignment work per area.
- Typed: declare `CustomTypeOptions` with `resources` typed from the English
  catalogs so `t('bad.key')` is a compile error and keys autocomplete.
  Validate that `translate()`/`t()` call sites reference existing keys; the
  typing does this for TS call sites, the check script covers `<Trans>` and
  dynamic keys.

Plurals: every count goes through a plural key (`_one/_few/_many/_other` as
each language needs; Russian needs all four, German and English `_one/_other`).
No suffix tricks, no `${n} thing${s}`. Audit every count site on main.

Dates, times, numbers: exclusively through `Intl.DateTimeFormat`,
`Intl.RelativeTimeFormat`, `Intl.NumberFormat`, always given the *selected*
i18n language, never the bare browser default. One small `formatters.ts` that
takes the language from i18n so callers do not pass locales around. Fix the
locale-sensitive month assertions in `dmFormatters.test.ts` by making the
test set the locale explicitly.

Loading: English bundled as the fallback; other locales lazy `import()` per
namespace via a small resource loader. Vite code-splits them.

Server errors: today routes send `{ error: 'English text', statusCode }` and
the client throws `HttpError(status, message)`. Add stable error codes in
`packages/shared` (an `ErrorCode` string union) and have routes send
`{ error, code, statusCode }` with `error` kept for older clients. The client
maps `code` to a localized message and falls back to `error`. This is a
shared-contract change; keep it backward compatible in both directions because
desktop app and instance version independently and federation relays errors.
Do the mapping for the error sites users actually see (auth, joins, uploads,
friends, DMs); document the rest as English-passthrough.

System messages: DM system messages are already structured
(`dmFormatters.ts` renders from `data.targetDisplayName`, `reason`, etc.), so
client-side localization is legitimate there. Confirm space/channel system
messages are also structured, not stored English text. If any are stored as
text, that is a server change to structure them, and it must be
federation-safe (relayed as structured events).

Desktop main process: tray/app menu labels (`recovery.ts`), `recovery.html`,
notification titles. The renderer tells main the selected language over IPC
(`set-language`), main keeps a tiny catalog for its handful of strings, and
falls back to `app.getLocale()` before the renderer has said anything.

Translation workflow: catalogs stay Weblate-compatible JSON. No hosted platform
set up in this pass; note it as a follow-up. `check:i18n` rewritten to enforce:
parity across all shipped locales, placeholder parity, no untranslated values
outside an allowlist, no count interpolation outside a plural key, and no
literal user-facing strings in JSX (heuristic, allowlisted).

Language selector in settings: driven by `supportedLanguages[].nativeName`,
shows each language in its own name.

RTL: not needed for en/ru/de; set `dir` from a per-language flag anyway so it
is one line when Arabic or Hebrew arrives.

## Step 4: execution order

1. Redo the two failed inventories (date sites, count sites) with quoted globs.
2. Write `docs/systems/localization.md` (the spec above, in the house voice).
3. Foundation PR: i18n setup, typed resources, loader, formatters, check
   script, `packages/shared` error codes, desktop IPC, language selector, plus
   one reference surface (settings) fully converted in en/ru/de. Tests for the
   loader, formatters, plural helper, error-code mapping, check script.
4. Sweep PRs per surface, each with en/ru/de catalogs, each reviewable on its
   own. Russian values lifted from `pr45:packages/web/src/locales/ru/translation.json`
   by matching English text; German written fresh and flagged for a native
   pass.
5. Close #45 with a comment that explains the structural reasons plainly,
   thanks st7105, says their Russian is being carried over with credit, and
   invites them to review the Russian result. Comment on #33 to d00xD that it
   is happening. Do this when the foundation PR is open, not before, so the
   closure points at real work.
6. Release 1.1.0 when the sweep is complete.

## Facts worth not rediscovering

- Repo has no `ErrorCode` anything in `packages/shared` today.
- `packages/web/src/api/client.ts:384` is where the client turns a failed
  response into `HttpError(status, body.error)`.
- Fork PR workflow runs need approval per push; recipe in memory
  `flatpak-packaging-pr48`.
- Local Node is 25; server tests need the `better-sqlite3` prebuild trick in
  memory `running-server-tests-node`. Web and desktop tests run as is.
- Never commit as anything but `151788261+TheZwiss@users.noreply.github.com`.

## Overnight run, 2026-09-04 → 05 (Jannis asleep; autonomous)

State at handoff: 1.0.6 published. #94 merged. #95 (foundation) in CI with a
background watcher that squash-merges it with the co-author trailer from
`/private/tmp/claude-501/-Users-jbraun-backspace-public/ee4f084d-af81-42c6-a54e-106608138e59/scratchpad/pr95-squash-body.md`. Flathub: NOT submitted, and must not be opened by
an agent (Flathub's generative AI policy forbids AI-generated or automated
submissions; the disclosure is Jannis's to write). A prepared branch sits in
`/private/tmp/claude-501/-Users-jbraun-backspace-public/ee4f084d-af81-42c6-a54e-106608138e59/scratchpad/flathub-submit/repo`; fork `TheZwiss/flathub` exists with nothing pushed.

Do overnight, in this order:
1. When #95 is merged: launch the eleven sweep forks below in parallel, each
   `subagent_type: fork`, `isolation: worktree`, prompt = "Read the sweep
   contract in this plan file (section below), your group is <name>,
   namespace <ns>, files <list>, sub-object ownership <...>". Contract also
   at `/private/tmp/claude-501/-Users-jbraun-backspace-public/ee4f084d-af81-42c6-a54e-106608138e59/scratchpad/sweep-contract.md`; the Russian lookup at
   `/private/tmp/claude-501/-Users-jbraun-backspace-public/ee4f084d-af81-42c6-a54e-106608138e59/scratchpad/ru-by-english.json` (regenerate from `pr45:packages/web/src/locales/`
   with the flatten-and-map script if the scratchpad is gone).
2. For each finished fork: `gh pr create` from its pushed branch
   (`i18n/sweep-<group>`), body in plain prose naming the namespace and the
   fork's report items, ending with the session URL. Squash-merge when CI is
   green, with `--body-file` carrying `Co-authored-by: st7105 <st7105@gmail.com>`
   (every sweep carries lifted Russian). Never pass `--subject`.
3. Conflicts between sweeps sharing a namespace (chat, spaces): deep-merge
   the JSON (the python `merge()` used for settings.json) and re-run the
   check. After each merge, on main: `node scripts/check-i18n.mjs
   --write-pending`, commit the regenerated list with the next PR or as
   `chore(i18n): regenerate the pending list`.
4. Consolidate every "wanted in common" item from the reports into
   common.json (en/ru/de) in one small PR at the end, pointing the
   namespaces at the common keys.
5. Server error codes: one PR per route file (auth.ts, spaces.ts, dm.ts,
   social.ts, uploads) converting user-visible sites to `sendError`, adding
   codes to `packages/shared/src/errors.ts`, `httpErrors.ts` and the three
   errors.json, with tests. Client sites already use `describeError`.
6. Review PR #93 only if gr1n-the-big-floppa has answered with the runtime
   observation; merge it if the observation is concrete. Otherwise leave it.

Do NOT overnight: flip `released` for ru/de; tag or publish anything; touch
Flathub; comment on #45/#33 again; deploy to nova/orbit. German needs a
native pass before 1.1.0; write the 1.1.0 PR (flags, release notes draft)
only as an unmerged PR for Jannis to read in the morning if everything else
is done.

Morning report for Jannis: what merged, what is open, pending-list count,
anything a fork flagged as a product bug, and the German phrases forks were
least sure of (collected from the reports).

### Sweep groups (name|namespace(s)|files under packages/web/src)

```
auth|auth|components/auth/LoginPage.tsx components/auth/RegisterPage.tsx components/JoinPage.tsx App.tsx
chat-messages|chat|components/chat/Message.tsx components/chat/MessageList.tsx components/chat/MessageInput.tsx components/chat/messageMenuItems.tsx
chat-widgets|chat|components/chat/AttachmentProgress.tsx components/chat/AttachmentRenderer.tsx components/chat/EmbedRenderer.tsx components/chat/GifPicker.tsx components/chat/ImagePreview.tsx components/chat/MentionPopover.tsx components/chat/SpaceInviteCard.tsx components/chat/SearchPopover.tsx
dm|dm|components/chat/DmDeletedNotice.tsx components/layout/DmMemberRow.tsx components/layout/DmRosterPanel.tsx components/layout/DmSearchBar.tsx components/layout/MobileDmsScreen.tsx components/layout/MobileGroupDmInfo.tsx components/modals/AddDmMemberModal.tsx components/modals/GroupDmSettings.tsx components/modals/NewDmModal.tsx components/modals/TransferOwnershipModal.tsx utils/dmFormatters.ts
voice|voice|components/voice/ConnectionInfoPopover.tsx components/voice/IncomingCallModal.tsx components/voice/MobileVoiceJoinSheet.tsx components/voice/ScreenSharePicker.tsx components/voice/ScreenShareSettingsPopover.tsx components/voice/StreamTile.tsx components/voice/VoiceChatPanel.tsx components/voice/VoiceControlBar.tsx components/voice/VoiceControls.tsx components/voice/VoiceGrid.tsx components/voice/VoiceUser.tsx components/voice/VoiceUserRow.tsx components/voice/voiceMenuItems.tsx components/layout/MobileVoiceFullScreen.tsx
spaces-layout|spaces|components/layout/ChannelSidebar.tsx components/layout/SpaceSidebar.tsx components/layout/MainContent.tsx components/layout/MobileSpacesScreen.tsx components/layout/MobileFolderSheet.tsx components/layout/MemberSidebar.tsx components/layout/MobileMembersScreen.tsx
spaces-modals|spaces|components/modals/CategorySettingsModal.tsx components/modals/ChannelSettingsModal.tsx components/modals/CreateCategory.tsx components/modals/CreateChannel.tsx components/modals/CreateSpace.tsx components/modals/InviteModal.tsx components/modals/JoinSpace.tsx components/modals/SpaceSettings.tsx components/modals/ExploreSpacePreviewCard.tsx components/chat/ExplorePage.tsx components/modals/spaceSettingsPanels/BansPanel.tsx components/modals/spaceSettingsPanels/MembersPanel.tsx components/modals/spaceSettingsPanels/OverviewPanel.tsx components/modals/spaceSettingsPanels/RolesPanel.tsx components/ui/PermissionsEditor.tsx components/ui/OverrideEntry.tsx components/ui/TriStateToggle.tsx
social|social|components/chat/FriendsPage.tsx components/modals/UserProfileModal.tsx components/ui/UserProfilePopout.tsx components/ui/AvatarStack.tsx components/layout/ActivityPanel.tsx utils/friendErrors.ts
federation|federation|components/modals/ConnectedInstances.tsx components/layout/MobileInstancePanel.tsx components/modals/instanceSettingsPanels/FederationPanel.tsx
admin|admin|components/modals/instanceSettingsPanels/GeneralPanel.tsx components/modals/instanceSettingsPanels/RegistrationPanel.tsx components/modals/instanceSettingsPanels/StoragePanel.tsx components/modals/instanceSettingsPanels/StreamingPanel.tsx components/modals/instanceSettingsPanels/UpdatesPanel.tsx components/modals/instanceSettingsPanels/UsersPanel.tsx
shell|uploads mobile desktop common|components/layout/TransferIndicator.tsx components/ui/ImageCropModal.tsx components/ui/Modal.tsx components/ui/SourceCodeLink.tsx components/ui/UpdateToast.tsx components/ui/ContextMenuRenderer.tsx components/layout/MobileShell.tsx components/layout/MobileSettingsScreen.tsx components/layout/MobileYouScreen.tsx components/layout/MemberListToggleButton.tsx components/layout/AppLayout.tsx components/modals/DeleteAccountModal.tsx
```

Sub-object ownership for shared namespaces: chat-messages owns
`chat.message`, `chat.list`, `chat.composer`, `chat.menu`; chat-widgets owns
`chat.attachment`, `chat.embed`, `chat.gif`, `chat.preview`, `chat.mention`,
`chat.invite` and the whole `search` namespace. spaces-layout owns
`spaces.sidebar`, `spaces.channels`, `spaces.members`, `spaces.mobile`,
`spaces.main`; spaces-modals owns `spaces.create`, `spaces.settings`,
`spaces.invite`, `spaces.join`, `spaces.explore`, `spaces.roles`,
`spaces.permissions`, `spaces.bans`, `spaces.category`, `spaces.channel`.
The shell group may edit common.json (it owns `uploads`, `mobile`,
`desktop` and common leftovers); no other group may.

### Sweep contract (verbatim, given to every fork)

# Localization sweep contract (read fully before touching a file)

You are a fork in an isolated git worktree. First run `git log --oneline -1`
and confirm the commit is on `main` at or after the merge of PR #95
("feat(i18n): localization foundation with English, Russian and German").
If the worktree is older, `git fetch origin && git reset --hard origin/main`.
Run `pnpm install` if node_modules is missing. Create a branch named
`i18n/sweep-<group>` from that commit.

The spec is docs/systems/localization.md. Read it, then read
packages/web/src/i18n/*.ts, packages/web/src/locales/en/common.json and
packages/web/src/locales/en/settings.json (a finished surface) so you use
the real API and match the established key style.

## What you change

- Only the files listed for your group, plus `packages/web/src/locales/{en,ru,de}/<your namespace>.json`.
- Do NOT edit: common.json, errors.json, settings.json, any other namespace,
  `packages/shared`, `packages/server`, `scripts/i18n-pending.txt`,
  `scripts/i18n-allowlist.json`, `resources.ts`. Eleven sweeps run in
  parallel; those files are how they would collide. If you genuinely need a
  new `common:` key, add the string to your own namespace for now and list
  it in your report under "wanted in common" so the maintainer consolidates.
- If two groups share a namespace (chat, spaces), each owns the top-level
  sub-objects assigned to it and adds nothing outside them; the files are
  deep-merged at merge time.
- Server error strings shown via `err.message`: replace with
  `describeError(err)` from `src/i18n/errors.ts` (it localizes by code when
  the server sends one and falls back to the server text). Do not add error
  codes; that is a separate pass.

## Conventions (the check script enforces most of these)

- `const { t } = useTranslation(['<ns>', 'common'])` in components; keys
  `<ns>:<element>.<meaning>`, semantic, camelCase, never the English text,
  never the file name. Reuse `common:actions.*` / `common:states.*` for
  Save, Cancel, Close, Delete, Confirm, Copy, Copied, Retry, Back, Done,
  Edit, Remove, Add, Apply, Reset, Continue, Dismiss, OK, Search, Clear,
  Change, Refresh, Open link, Try again, Loading…, Saving…, Saved, Unknown,
  None, Enabled, Disabled, On, Off, Yes, No, Optional, Required, Default,
  Recommended.
- Module-level helpers and non-React utils use `i18n.t(...)` from
  `src/i18n` (default export) or take a `t` parameter.
- Every count → plural key with `{{count}}`: en/de `_one/_other`, ru
  `_one/_few/_many/_other`. No `${n} thing${s}`, no `x !== 1 ? 's' : ''`.
- Dates/numbers/bytes/percent → `useFormatters()` (components) or
  `formatters` (helpers). Never `toLocale*` / `Intl.*`.
- Strings with markup inside (`<strong>`, `<code>`, links) → `<Trans>` from
  react-i18next with named components, the way KeybindsPanel does it.
- No user-facing literal remains: JSX text, `placeholder`, `title`,
  `aria-label`, `alt`, toast messages, error strings set into state, labels
  in data arrays (menu items, tab lists, option lists). Technical
  identifiers, CSS, `console.*`, and stored data stay. A false positive of
  the literal check gets `// i18n-check: allow-literal` on the line above,
  with a reason.
- Do not restructure logic. Replace strings, formatting calls and plural
  constructions; nothing else. Keep existing tests green; tests run in
  English, so assertions on English text still pass. If a test asserts a
  string that you had to change (e.g. a plural that was wrong), fix the test
  and say so.

## Translations

- Russian: look each English string up first in
  /private/tmp/claude-501/-Users-jbraun-backspace-public/ee4f084d-af81-42c6-a54e-106608138e59/scratchpad/ru-by-english.json
  (st7105's translation from PR #45, keyed by English text). Use it when it
  is right and natural; fix it when it is not (known mistakes: "Clear" →
  "Прозрачный", "Apply" → "Применять", "Space" → "Пространство" in the
  keyboard sense, "Volume" → "объем"). Write the rest yourself in the same
  register as ru/common.json and ru/settings.json (neutral "вы"; UI nouns
  match: сервер for instance, пространство for space, канал, личные
  сообщения for DMs, друзья, трансляция for stream/screen share).
- German: write it fresh, natural, "du" register, matching de/common.json
  and de/settings.json (Instanz, Space, Kanal, Direktnachricht, Freunde,
  Stream/Bildschirmübertragung). Keep product and technical terms.
- Placeholders identical across languages for every key. Catalog files
  pretty-printed with 2-space indent, keys in a stable order.

## Verify, in packages/web

1. `npx tsc --noEmit` clean.
2. `npx vitest run` fully green.
3. `node ../../scripts/check-i18n.mjs` reports no findings. (Your files are
   still in the pending list, so rule 8 does not check them; run
   `node ../../scripts/check-i18n.mjs --write-pending` locally to see whether
   they drop out, then `git checkout -- ../../scripts/i18n-pending.txt` so
   you do not commit that change.) Every one of your files must drop out of
   the regenerated list; if one does not, fix the remaining literal.
4. `grep -nE "toLocale|Intl\." <your files>` empty.

Commit on your branch with a message `feat(i18n): localize the <group> surface`
and a body that names the namespace and anything a reviewer must know,
ending with `Claude-Session: https://claude.ai/code/session_01YLpoVDj4ABy62r3JjCwSiy`.
Push the branch (`git push -u origin i18n/sweep-<group>`). Do not open a PR.

## Report back

Branch and commit; number of keys per language; the "wanted in common"
list; Russian phrases from #45 you corrected; German you are least sure of;
any string that looked like a product bug (wrong plural, mismatched limit,
stale wording) and what you did with it; any test you changed.
