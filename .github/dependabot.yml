# Dependabot keeps dependencies and CI actions patched. Three ecosystems:
#   - npm      → the pnpm workspace (Dependabot reads pnpm-lock.yaml v9)
#   - github-actions → action version bumps (feeds the SHA-pin comments)
#   - docker   → the Dockerfile base image (FROM node:20-slim)
#
# NOTE (intentional): there is NO docker entry for docker-compose.yml. It sits
# at the same "/" directory (a second docker entry would collide on
# ecosystem+directory), and Dependabot's docker ecosystem parses Dockerfiles,
# not `image:` refs in compose. The pinned caddy / livekit-server compose images
# are updated MANUALLY — see the maintainer checklist in
# docs/systems/security-scanning.md.
version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 10
    groups:
      # One grouped PR for routine minor/patch bumps to cut PR noise.
      npm-minor-patch:
        update-types:
          - minor
          - patch
    ignore:
      # uiohook-napi is pinned by an exact-version pnpm patch
      # (patches/uiohook-napi@1.5.5.patch). A bump makes the patch path stop
      # matching, breaking `pnpm install --frozen-lockfile` in CI and both
      # Docker stages until the patch is regenerated. Bump it by hand.
      - dependency-name: uiohook-napi

      # ---------------------------------------------------------------------
      # Framework majors we have deliberately deferred.
      #
      # Each entry below ignores ONLY `version-update:semver-major`, so minor
      # and patch releases (including security patches) still flow through the
      # npm-minor-patch group as normal. Without these, Dependabot reopens the
      # same major PR every week: closing one only suppresses that exact
      # version pair, and the minor-patch group keeps moving the base version,
      # which makes the next weekly run see a brand-new pair.
      #
      # Each entry records what unblocks it. Do the upgrade as its own PR,
      # then delete the entry. Do not silently widen an entry to dodge a red
      # build.
      # ---------------------------------------------------------------------

      # React stays on 18. The move to 19 is a typed migration, not a bump:
      # `RefObject<T | null>` variance and `useState` arity break across the
      # chat components. Unblock: after the UI pass (#180) lands, in one PR
      # that moves react, react-dom and both @types packages together.
      - dependency-name: react
        update-types: ["version-update:semver-major"]
      - dependency-name: react-dom
        update-types: ["version-update:semver-major"]
      - dependency-name: "@types/react"
        update-types: ["version-update:semver-major"]
      - dependency-name: "@types/react-dom"
        update-types: ["version-update:semver-major"]

      # Tailwind stays on 3. Version 4 moves the PostCSS plugin into
      # `@tailwindcss/postcss` and replaces the JS config model that the
      # Aether Drift design system is built on (docs/systems/design-system.md).
      # Unblock: a dedicated PR that ports the theme, the surface/input tiers
      # and globals.css, verified against Backspace-design-prototype.html.
      - dependency-name: tailwindcss
        update-types: ["version-update:semver-major"]

      # TypeScript stays on 5. Version 7 removes the `baseUrl` compiler option
      # (TS5102), which the workspace tsconfigs rely on.
      # Unblock: a PR that migrates every tsconfig off baseUrl first.
      - dependency-name: typescript
        update-types: ["version-update:semver-major"]

      # Fastify core and its plugins move together or not at all. The 11.x
      # plugin lines target Fastify 5; on Fastify 4 the server stops booting.
      # Unblock: one PR that takes fastify 4 -> 5 and every @fastify/* plugin
      # in the same commit, with the federation and security-header suites green.
      - dependency-name: fastify
        update-types: ["version-update:semver-major"]
      - dependency-name: "@fastify/*"
        update-types: ["version-update:semver-major"]

      # better-sqlite3 stays on 12. Version 13 segfaults every spawned test
      # instance on boot (exit 139) against the node:20-slim base image, which
      # is a native ABI mismatch rather than a flake.
      # Unblock: when the Dockerfile base image moves off Node 20, re-test then.
      - dependency-name: better-sqlite3
        update-types: ["version-update:semver-major"]
      - dependency-name: "@types/better-sqlite3"
        update-types: ["version-update:semver-major"]

      # @types/node tracks the RUNTIME, not npm's latest. The image runs
      # node:20-slim and CI's baseline leg is Node 20, so types describing a
      # newer Node let code typecheck clean and fail at runtime. The two script
      # packages sit on 24 for the same reason: match what runs them.
      # Unblock: whenever a runtime moves, in that same PR.
      - dependency-name: "@types/node"
        update-types: ["version-update:semver-major"]

      # jsdom stays on 28. jsdom 30 pulls undici 8 while the pnpm overrides in
      # the root package.json pin undici@^7, leaving undici 6, 7 and 8 in one
      # tree; every vitest forks worker then dies with
      # `webidl.util.markAsUncloneable is not a function` and the web suite does
      # not run at all. It also changes SVG attribute-selector matching, which
      # breaks the checkmark lookups in ContextMenuCheckbox.test.tsx.
      # Unblock: extend the undici override to cover 8, then re-test both.
      - dependency-name: jsdom
        update-types: ["version-update:semver-major"]

      # vitest stays on 4 because @cloudflare/vitest-pool-workers supports
      # `vitest ^4.1.0` only and depends on internal Vitest APIs that carry no
      # semver guarantee. Under vitest 5 the receiver's Workers runtime throws
      # `jsg.SyntaxError` before a single test runs. This one is upstream's
      # call, not ours.
      # Unblock: when @cloudflare/vitest-pool-workers ships vitest 5 support.
      - dependency-name: vitest
        update-types: ["version-update:semver-major"]

      # electron stays on 43. Electron 44 removes the macOS login-item API the
      # main process uses (`openAsHidden` on Settings, `wasOpenedAsHidden` on
      # LoginItemSettings, src/main.ts), and an Electron major also moves the
      # Chromium and Node the shipped app runs on. That wants its own PR with a
      # real smoke test, not a dependency bump. Note a bump here also needs the
      # Flatpak offline sources regenerated or that leg fails on a missing
      # tarball. See docs/systems/desktop.md and docs/systems/desktop-security.md.
      # Unblock: a dedicated desktop PR porting the hidden-launch handling.
      - dependency-name: electron
        update-types: ["version-update:semver-major"]

      # The tus store and server are a matched pair: @tus/file-store 2.x wants
      # @tus/server 2.x, and bumping either alone splits the pair.
      # Unblock: one PR that moves both, with the upload pipeline exercised
      # end to end (docs/systems/uploads.md).
      - dependency-name: "@tus/file-store"
        update-types: ["version-update:semver-major"]
      - dependency-name: "@tus/server"
        update-types: ["version-update:semver-major"]

  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      github-actions:
        patterns:
          - "*"

  - package-ecosystem: docker
    directory: /
    schedule:
      interval: weekly
